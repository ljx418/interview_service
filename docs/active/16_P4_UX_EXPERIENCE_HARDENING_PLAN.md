# JobPilot AI P4 UX 体验优化开发及验收计划

日期：2026-06-16  
阶段：P4 候选 / 待启动  
来源：P3 人工审查反馈

关联审查报告：

```text
docs/active/stage-reviews/P4_UX_TEAM_AUDIT.md
```

## 1. 背景

P3 已完成本地自动化验收：Chatbox 响应闭环、示例/真实资料模式边界、对话区/推进台分离、响应式截图和 HTML 报告均已落地。

但人工审查结论是：

> 认可当前验收报告的大部分内容，但对当前用户体验不完全认同。后续开发阶段必须增加交互体验优化篇幅。

因此，P4 候选阶段应优先处理“真实用户是否愿意、是否顺手、是否理解下一步”的体验问题，而不是继续扩展 MCP、CLI、ASR 或会议平台。

## 2. P4 UX 目标

P4 UX 的目标不是让页面更复杂，而是让求职工作台更像一个可持续使用的工作流工具：

```text
用户打开 Chatbox
→ 清楚知道当前该做什么
→ 输入或上传后知道系统正在做什么
→ 能快速区分聊天、计划、产物、确认项和导出
→ 能在错误或缺资料时自然恢复
→ 能在窄屏和桌面上稳定完成同一条任务
```

## 3. 体验问题清单

P4 启动前需要重新截图审计，至少覆盖：

- 首屏信息密度是否过高；
- Chatbox 和推进台是否仍有认知负担；
- 快捷任务是否像真实操作入口，而不是临时按钮；
- 产物卡是否过度暴露 JSON / 版本细节；
- 用户是否能理解“示例模式”和“我的资料”的后果；
- provider configured 是否容易误解为“正在外呼”；
- 申请包、面试准备、复盘的下一步是否足够明确；
- 移动端是否只是“不坏”，但仍不好用；
- 空状态是否能引导用户开始，而不是展示工程状态；
- 错误态是否给出真实可执行恢复路径。

## 4. P4 UX 工作包

| 工作包 | 目标 | 交付物 | 验收证据 |
| --- | --- | --- | --- |
| P4-UX0 团队审查收口 | 将 P4 UX 团队审查问题转成设计和开发门槛 | 问题清单、优先级、before/after 截图计划 | `P4_UX_TEAM_AUDIT.md` 与阶段启动审计 |
| P4-UX1 信息架构重排 | 降低首屏认知负担 | Chatbox / Workbench 内容层级重排 | 桌面/移动截图对比 |
| P4-UX2 任务入口优化 | 让上传、粘贴 JD、生成申请包更像自然流程 | 明确的任务入口、下一步建议、空状态 | 有效/缺资料/完成态截图 |
| P4-UX3 产物卡可读性 | 减少 JSON 暴露，突出摘要、确认项和导出 | 产物卡信息层级、折叠规则 | 申请包/匹配报告/面试准备截图 |
| P4-UX4 状态与反馈 | 让用户知道系统正在做什么、失败后怎么办 | loading、progress、recoverable error | 错误恢复截图和 eval |
| P4-UX5 响应式精修 | 从“可用”提升到“顺手” | 390 / 720 / 1280 细节修正 | Chrome 截图和人工审查 |
| P4-UX6 人工体验审查包 | 让人工能快速判断体验是否改善 | HTML 对比报告 | 审查报告和风险清单 |

## 5. P4 UX 验收门槛

P4 UX 不通过条件：

- 用户仍然不知道第一步该做什么；
- 对话区和推进台仍让用户混淆；
- 缺资料、错误、处理中状态仍不够清楚；
- 产物卡主要依赖 JSON 才能理解；
- provider 状态被误解为默认外部调用；
- 移动端虽然不横向溢出，但核心操作仍费力；
- 报告只展示截图，不说明人工体验问题是否改善。

P4 UX 通过条件：

- 人工审查者认可核心路径的交互体验有明显改善；
- examples 路径和我的资料路径的启动方式清楚；
- 用户能在不读开发文档的情况下完成“导入资料 → 粘贴 JD → 生成申请包 → 查看确认项 → 导出”；
- 自动化测试、前端 build、Chrome 截图和人工审查记录都通过；
- 未把真实个人资料或真实外部 provider 调用写成未执行的验收。

## 6. 非目标

P4 UX 不默认包含：

- MCP Server；
- CLI；
- ASR / Whisper；
- 会议平台接入；
- 自动海投；
- SaaS 登录 / 多租户 / Billing；
- 发布级复杂设计系统；
- 全量重写前端。

这些能力若要进入 P4，必须重新制定阶段 PRD、目标架构、验收门槛和高风险确认流程。

## 7. 启动审计意见

当前建议：

```text
P3 可以保持“本地自动化验收通过”的结论；
但 P3 不应被描述为“人工体验已认可”；
P4 候选阶段应优先做 UX 体验优化，而不是扩展新入口。
```

P4 启动前需要完成：

- 基于当前 P3 截图的人类体验问题清单；
- P4 UX 设计/交互验收标准；
- P4 UX before/after 截图计划；
- 是否允许继续使用 examples 作为主要验收数据的确认；
- 是否需要真实个人资料受控体验审查的确认。
